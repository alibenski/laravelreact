import React, { Component } from "react";

const FormUserDetails = ({ nextStep, handleChange, values, continueNext }) => {
    return <div>Form User Details</div>;
};

export default FormUserDetails;
